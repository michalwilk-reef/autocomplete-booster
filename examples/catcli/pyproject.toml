[project]
name = "fastcomplete-catcli-example"
version = "0.3.0"
description = "Small cat picture CLI demonstrating fastcomplete integration"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32",
    "Pillow>=10",
    "fastcomplete @ git+https://github.com/michalwilk-reef/autocomplete-booster.git@8418a133a8d916bf6612d87279736cdb5a61bfa0",
]

[project.scripts]
catcli = "catcli.entry:main"
catcli-argcomplete = "catcli.cli:baseline"

[build-system]
requires = [
    "setuptools>=77",
    "requests>=2.32",
    "Pillow>=10",
    "fastcomplete[build] @ git+https://github.com/michalwilk-reef/autocomplete-booster.git@8418a133a8d916bf6612d87279736cdb5a61bfa0",
]
build-backend = "setuptools.build_meta"

[tool.setuptools.cmdclass]
build_py = "fastcomplete.setuptools.BuildPy"

[tool.fastcomplete]
entrypoint = "catcli.entry:main"
